
githubPages {                                        
  repoUri = "https://github.com/aestasit/${githubProjectName}.git"
  pages {
    from(groovydoc.outputs.files) {
      into 'docs/groovydoc'
    }
  }
}